version: '3.8'

services:
  qwen-api-wrapper:
    build: .
    container_name: qwen-api-wrapper
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8220:8220"
    command: uvicorn main:app --host 0.0.0.0 --port 8220
